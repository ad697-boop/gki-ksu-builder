5796380 feat: 新增 AviumUI-16.2.1-cupid ROM 预设（rom_preset 参数，官方 SourceForge 直链）
fe577af fix: SUSFS 构建固定 builtin@6c13a069（KSU_APP_PROFILE_VER=3，匹配 v4.1.3 管理器，修复 app profile 更新失败）
2479f30 docs: 增加 AGENTS.md 与完整文档（供其它 AI 接手），不推送
f9b7ad1 fix: 删除过期的 builtin USER_ARG_NULL sed 补丁（上游已修复，旧补丁反而改坏）
a0f6d7b revert: 只保留 lineage-23.2，移除 EvolutionX config 模式与配置文件
f06328a feat: 增加 susfs 开关（builtin 分支 + susfs4ksu 5.10 内核补丁 + CONFIG_KSU_SUSFS）
f15b089 feat: 支持 config 构建模式（免 ROM 直链）+ EvolutionX cupid 5.10.247 配置
63dd62b fix: LKM 构建前跑 modules_prepare 生成 module.lds，并用 vmlinux.symvers 组装 Module.symvers
fd38b13 fix: ReKernel-X LKM 需在内核树 drivers/rekernel_x 下编译（相对 include 依赖）
294e00a feat: 编译 ReKernel-X LKM 并打包匹配当前内核的模块（修复官方 ko vermagic 不匹配导致无效）
0741423 feat: 默认 pin SukiSU v4.1.3 并强制驱动版本 40796（与管理器一致）
e4db466 fix: 抑制 setlocalversion 脏树 + 标记（export LOCALVERSION=空），版本校验改为 vermagic 精确匹配
a4403ab fix: 解包后把原厂 boot.img 复制进工作目录，repack 才能找到它
71b9ece fix: 换用官方 Magisk APK 的静态 magiskboot（Rust 版 repack 有 ENOENT bug），repack 失败时打印完整日志
8ad839f fix: runner 自带 /swapfile，先 swapoff -a 再重建 8G swap
f9b4c7a fix: 加 8G swap 防 OOM，full LTO 改 thin LTO，构建日志 always 上传
4cc4a50 fix: 默认切到 SukiSU main 分支（builtin 在 5.10 有 sulog 类型 bug），并加 builtin 兼容补丁
a29eed6 fix: SukiSU 软链路径少一层目录（drivers/kernelsu -> ../KernelSU/kernel）
474f40a fix: magiskboot 日志输出到 stderr，改为 2>&1 收集后再校验
0aacf51 fix: 容忍 magiskboot 的 vbmeta ASN.1 警告（非致命），以产物文件存在为准
2e2e6f2 fix: payload-dumper release tag 去掉 v 前缀（2.0.2 而非 v2.0.2）
3ea9e1b fix: 使用 ssut/payload-dumper-go v2.0.2（CLI 兼容 -partitions boot）
7691dcf sm8450: 新增 SukiSU 内置内核构建 workflow（lineage-23.2 / cupid）
b77eb40 fix: set fixed version suffix instead of blanking setlocalversion output
5da581d fix: strip dirty + suffix from kernel version and properly disable KMI symbol trimming
2aff3f7 fix: force ROM kernel version string and disable KMI symbol trimming for vendor module compatibility
be7467a fix: use thin LTO to avoid OOM on GitHub runners
c1dfc08 fix: skip check_defconfig (KSU default y conflicts with savedefconfig)
8b3cdbb build: fall back to official 2025-12 GKI release when exact commit unreachable
ab168dc fix: add tag fallback for pinning kernel commit
c945314 fix: use shallow-since (time-based) fetch to reach Dec 2025 commits
e408713 fix: fetch branch history via full URL instead of origin remote
7b9d6cd fix: fetch branch history before checking out pinned commit
e982904 fix: export PATH within same step before running repo --version
6ef4d9a GKI 5.10 + KernelSU builder for LMODroid cupid
